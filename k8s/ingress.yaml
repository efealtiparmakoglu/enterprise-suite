apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-suite-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: enterprise-suite.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-suite-service
            port:
              number: 80
